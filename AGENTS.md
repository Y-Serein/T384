# AGENTS.md

本文件是 `/home/slam/Sipeed/T384` 的项目级规则。全局规则继续适用；本文件只补充 T384/T640 热成像插件的项目边界、资料优先级、硬件协议和验证要求。

---

## 1. 项目概览

- 项目目标：基于 CH32H417 V5F 接收 MINI2 384/640 的 DVP 数据，通过 USB NCM 提供本地 Web 测温与成像界面。
- 当前阶段：正式firmware已按用户B方案运行真实MINI2双核链路，V5F采集/V3F网络。双帧v2两核启动及五段内存访问自检通过，Windows原生60秒1174完整帧/19.567FPS，用户目标≥25完整FPS尚未达成。当前继续TCP复制效率与持续流/重连闭环；不能把DMA等效源历史27.750FPS成绩当真实MINI2成绩。
- 正式测温、手机和其他PC系统、24小时尚未验证，不能写成T384/T640产品完成。当前实现和内存账本见firmware/README.md，最新状态见HANDOFF.md。
- 目标平台：iPhone、Android 和 PC；具体 PC 系统矩阵尚未冻结。
- 当前产品范围：T384/T640；除非用户明确变更，不启动 256×192 SKU。
- 优先级：电气安全与数据正确性 > 协议合法性与跨平台兼容 > 可验证性 > 小改动 > 吞吐与延迟 > UI/重构。

不要把本项目当成：

- 已有可编译固件的成熟仓库。
- 普通 UVC 摄像头项目；产品目标当前是 NCM + Web。
- 纯前端项目；DVP 时序、电平、DMA、USB 和测温表均是核心风险。
- 可以直接在 `docs/data/` 厂商示例里持续开发的工程。

## 2. 开工流程

开始修改前依次检查：

1. `README.md`
2. `HANDOFF.md`
3. `docs/design/T384_640 热成像插件.md`
4. 当前任务直接相关的接口表、协议表、SDK 或 EVT 示例
5. `/home/slam/Sipeed/C_context/KNOWN_FAILURES.md`

当前通用 preflight 脚本没有 `T384` 项目项。不要借用 `pico-tn160`、`nanoups` 等名称运行后，把结果当作本项目状态。

硬件、USB、DVP、DMA、协议或测温改动必须先给 3-6 条简短计划，说明当前行为、目标行为、修改层级、最小验证和未上板风险。

## 3. 常用命令

### 安装依赖

当前没有项目级安装命令。不要自动联网安装 WCH 工具链、Python/Node 依赖或系统包。

### 构建

当前正式 RAW16 工程入口为 `firmware/T384-RAW16-BENCH.wvsln`；已验证阶段工程保存在
`tests/ch32h417 t384 raw16 bench/`，NCM/OV2640 保护基线保存在
`tests/ch32h417_t384_ncm/`。静态检查分别为
`bash tools/check_ncm_firmware.sh` 和 `bash tools/check_raw16_bench.sh`。
`docs/data/EVT/` 中的 `.wvsln` / `.wvproj` 属于厂商参考工程，构建它们不等于构建本项目。

当前 NCM 与 RAW16 主工程使用 MRS 的 `CH32H417WEU` 芯片项，对应目标器件
`CH32H417WEU6`；该项已由 WCH Petros_DVP 官方板工程和本地 V1.6 数据手册交叉确认。
用户已确认B方案：正式RAW16工程Build V3F网络核与V5F采集核；已验证测试快照仍只构建V3F。双核已完成MRS目标构建/合并产物检查，用户已上板出图；单帧HTTP稳定性修复60秒/3次重连通过，双帧v2完整帧19.567FPS未满足≥25FPS。新优化必须确认实际下载身份再测，不沿用旧产物放行。两者均禁止 `Erase All` 与 `Clear CodeFlash`；
`tests/ch32h417_t384_base/` 仍保留厂商 QEU 参考元数据，不得作为目标板下载工程。

MRS 工程的 `.wvsln/.wvproj/.project/.cproject` 必须使用相对 linked folders，不得写入
盘符或工作区绝对路径。`.mrs/` 会被IDE自动写入当前机器绝对路径，和 `V3F/obj/` 一样是
本机生成缓存，不属于可迁移源码；迁移
后只需一次性丢弃旧缓存，日常源码/头文件修改使用增量 Build，不要求每次 Clean。

已完成双核目标构建闭环：

- MRS 2.5.0；现有WCH xPack RISC-V Embedded GCC 12.2.0，两个核使用RV32IMAC/WCH扩展及有效O2。
- V5F采集DVP/DMA/UART，V3F处理USB NCM/lwIP/HTTP；测试快照仍为历史单核。
- MRS先生成两核obj makefile后，Windows项目目录运行 `powershell -NoProfile -ExecutionPolicy Bypass -File tools/build_dualcore.ps1`；脚本使用已安装MRS工具链重建、合并并检查，不下载/擦除。
- 产物：`firmware/{V3F,V5F}/obj/T384-RAW16-BENCH_{V3F,V5F}.{map,hex,bin}`；双核合并文件 `firmware/V5F/obj/Merge.bin`。

### 测试与静态检查

当前已有 `bash tools/check_ncm_firmware.sh` 和 `bash tools/check_raw16_bench.sh`，
用于主机 GCC 严格语法检查、工程 JSON/XML、USB 描述符与 RAW16 队列/协议 smoke test；
它们不等于 WCH 目标编译或上板验证。当前没有项目级 lint/format。

不要为了建立“看起来完整”的规则而编造命令。

## 4. 目录与文件边界

- `docs/design/`：项目自有产品设计、架构和附件，修改前确认是否为当前基线。
- `docs/design/图片和附件/`：原始附件。默认不改、不覆盖、不原地解压。
- 根目录 `designs/` 禁止创建。UI原型、设计系统、design Skill/工具生成物必须放在
  `docs/design/` 的明确子目录；即使外部Skill默认使用 `designs/`，也必须由本项目路径规则覆盖。
  不要用 `.gitignore` 隐藏违规目录，发现后先确认内容，再迁移到 `docs/design/`。
- `docs/data/EVT/`：CH32H417 厂商资料、EVT 和参考代码。默认只读。
- `docs/data/SDK/`：机芯厂商 SDK。默认只读。
- `README.md`：稳定的项目入口和已确认边界。
- `HANDOFF.md`：当前 session 状态，不替代长期设计文档。

未来新增内容优先使用：

- `firmware/`：唯一正式开发固件源码和MRS工程，不再放备份或阶段验证副本。
- `web/`：设备内嵌或配套 Web 前端。
- `tests/`：主机测试，以及已通过阶段验证的完整固件工程快照。
- `tools/`：抓流、表转换、测速和调试工具。
- `out/`：临时生成结果。
- `docs/reference/`：提炼后的外部协议和资料索引。
- `docs/runbooks/`：稳定的联调、烧录和排障流程。

从厂商示例移植代码时，只复制必要部分，记录来源、版本、许可证和本项目修改点。WCH 示例头部限制其代码/二进制用于 WCH MCU，不得忽略。

## 5. 证据优先级

发生冲突时按以下顺序判断：

1. 真实硬件测量、逻辑分析仪/示波器和可复现主机日志。
2. 当前项目主线源码、原理图和冻结接口定义（建立后）。
3. 用户明确确认的产品要求。
4. MINI2 硬件接口表和串口/I²C 指令表。
5. AC020 SDK 和 CH32H417 官方资料/EVT。
6. AX620Q 原理图、`web.zip` 等相邻项目或历史原型。

资料有矛盾时必须显式记录，不得静默选一个：

- 设计草案概括“IO 1.8V”，接口表则区分 DVP/I²C 默认 1.8V、UART 3.3V。
- `SCH_XDV_SOC1...pdf` 是 AX620Q 参考原理图，不是 CH32H417 目标板证据。
- `web.zip` 针对 256×192 iRay Linux 原型，不是 T384/T640/NCM 实现。

## 6. 硬件与协议红线

### MINI2 接口

- 机芯主电源资料为 `5V ±10%`，启动探测器约有 `500mA` 浪涌；电源设计必须留瞬态余量并上板测量。
- 50Pin 逻辑连接器之外还有连接器外壳/机械 GND，不要把它们误编号成新的逻辑信号。
- DVP/I²C 域默认上电为 `1.8V`。在电平切换命令成功且双方状态已确认前，不得由 CH32H417 侧驱动 3.3V。
- UART、复位、工作模式等引脚必须逐信号核对电压，不能套用“全部 1.8V”或“全部 3.3V”。
- DVP 数据线是 8-bit `DATA0..7`（连接器名 `DVP_DATA8..15`）。RAW16 如何跨两个字节传输、采样沿、极性、行场消隐和信息行都必须由资料或逻辑分析确认。
- USB-C 公头方向、摄像头竖屏方向和 USB 3.x 翻转 switch 属于硬件接口，不得仅凭外观决定布线。

### 控制协议

- UART 基线：`115200 8N1`、无流控。
- I²C 模组地址：`0x3C`（7-bit）；`0x78/0x79` 只表示 8-bit 写/读地址。代码和文档必须注明地址口径。
- I²C 命令 buffer 起始地址为 `0x1D00`，状态寄存器为 `0x0200`；修改前回看协议表的分包/中断位说明。
- 串口命令固定头为 `55 43 49 12 00`，命令数据使用 CRC-16/XMODEM。不要手抄大量十六进制常量后假设 CRC 正确，必须用测试向量验证。
- 改变命令格式、表格式、VID/PID、USB 描述符、端点、NCM 参数或浏览器 API 均视为协议变更，先说明兼容性影响并等待确认。

### 数据与测温

- 已知算法草案：`corrected_nuc = BT[i] + ((KT[i] * raw_nuc + 8192) >> 14)`；`KT` 是 Q14，`BT` 按有符号 16-bit 解释。
- Advanced NUC-T 草案为 16384 个 `uint16_t`，表值是开尔文温度乘 16；旧版可能只有 8192 项。使用前必须识别表版本并做边界 clamp。
- KT、BT、NUC-T、距离修正约 `55 KiB/增益档`，两个增益小于 `128 KiB`；空间 K、RMCOVER、DPC 的实际存储归属仍需确认，不能重复计入或遗漏。
- 不得用 `web.zip` 的经验线性温度公式代替正式 KT/BT/NUC-T 辐射测温链路。
- 测温结果受 FFC、机芯温度、增益档、距离、发射率、反射温度和标定数据影响；没有黑体/参考温度实测时不能宣称精度。

### USB 与网络

- 当前产品目标是 USB NCM + 本地 HTTP/Web，不得因为厂商有 UVC-DVP 示例就静默改成 UVC 产品方案。
- 当前资料中未找到 CH32H417 官方 NCM 示例；仓库现有 NCM 主线是基于 TinyUSB 和
  H417 EVT USBHS 寄存器自行移植的未上板实现，不得当作 WCH 已验证能力或产品兼容性证据。
- 当前开发测试身份为 `VID/PID=0x1A86/0xE384`，仅用于隔离驱动缓存，禁止发布；正式
  USB 身份冻结后必须重新验证 Windows/Linux/macOS/Android/iPhone 枚举和驱动缓存。
- 640×512×16-bit 原始流是 `16.384 MB/s`（25 fps）或 `32.768 MB/s`（50 fps），另有协议开销。必须测持续吞吐、帧序号、丢帧、缓存水位和断连恢复。
- “兼容 iPhone/Android/PC”只有在真机枚举、网络配置、页面打开、持续流和重连均验证后才能写成完成。
- 将公共域名映射到设备私有 IP 涉及 DNS、HTTPS 证书、浏览器安全策略和联网/离线冲突；未经方案评审不得作为默认实现。
- 本地 HTTP 服务默认不应暴露到非 USB 网络接口；任何远程访问、遥测或云服务都需要用户明确授权。

## 7. 代码修改规则

- 只做当前任务需要的最小改动，不混入无关重构、格式化或依赖升级。
- 先建立一条最小闭环：固定分辨率/帧率 DVP 抓取 -> 帧完整性 -> USB 传输 -> 浏览器显示，再扩展双 SKU、双增益和测温。
- 中断/DMA 路径禁止阻塞、动态分配、长日志或无界循环；明确缓冲区所有权、对齐、cache 一致性和溢出策略。
- 不凭经验猜寄存器、GPIO 复用、DVP 极性、USB 描述符或时序。
- 厂商 UVC-DVP 示例存在复制粘贴式注释和固定 OV2640/RGB565/MJPEG 假设，移植前必须逐项审计，不能整目录照搬。
- 前端不得把超分、插值或伪彩生成值反写为测温数据；温度统计必须基于原始数据。
- 不把 secrets、网络凭据或生产标识写进源码、文档或日志。

## 8. 验证要求

按修改层级做最小闭环：

| 修改层级 | 最小验证 |
|---|---|
| 文档/规则 | 读回、路径存在性、术语/单位一致性 |
| 机芯控制协议 | 已知 CRC 测试向量、命令回执、超时/错误码、7-bit 地址核对 |
| DVP/电平 | 上电电压、时钟/行场/采样沿、连续帧长度、溢出计数 |
| DMA/缓存 | 对齐与 cache、双缓冲所有权、压力下无越界/撕帧 |
| USB NCM | 描述符合法、各目标主机枚举、IP/DHCP、持续吞吐、断连恢复 |
| Web/测温 | 原始数据不丢精度、表版本、边界值、黑体/参考温度回归 |
| 整机 | T384/T640 分别验证，25/50 fps 分档记录功耗、温升和丢帧 |

最终回复必须列出实际运行的命令/测试、结果和未验证项。没有真实板卡时明确写“未上板验证”。WSL/Linux 结果不能替代 iPhone、Android、Windows 或其他目标主机真机验证。

## 9. 禁止自动执行

除非用户明确要求，不要自动：

- 初始化 Git、commit 或 push。
- 联网下载或安装工具链/依赖。
- 修改、删除、移动或批量格式化 `docs/data/` 和原始附件。
- 原地解压 `web.zip` 或把其内容当成主线源码。
- 改变通信协议、文件/表格式、硬件接口、电平、VID/PID 或 USB 类别。
- 生成生产固件、烧录、擦除或操作真实设备。
- 启动长期服务或把本地服务暴露到局域网。

## 10. 完成与沉淀

- 按用户要求，小范围代码/脚本修复只修改必要实现和相关验证，不自动同步 README、流程手册、迭代日志、HANDOFF 或项目记忆。
- 不自动创建或恢复已删除的标定手册和产品迭代日志。只有用户明确要求记录、交接或修改文档时，才更新对应文档；本约定覆盖全局规则及 Skill 的自动记录要求。
- `HANDOFF.md` 只在用户明确要求交接时更新，不追加每次小修复的过程记录。
- HANDOFF 使用固定结构：当前状态、已尝试及结果、3-5 条下一步、关键相对路径、验证状态、未决问题。
- 文档不得把“目标”“厂商示例可行”“静态检查通过”写成“产品已完成”或“已上板验证”。
